"use client";import {History} from "lucide-react";import {useUMLStore} from "@/stores/uml-store";import {useLocale} from "@/lib/i18n";
export function VersionPanel(){const h=useUMLStore(s=>s.history),restore=useUMLStore(s=>s.restore);const {t}=useLocale();return <div className="border-t bg-neutral-50 p-2"><div className="mb-1 flex items-center gap-1 text-[11px]"><History size={12}/>{t("version.history")}</div><div className="flex gap-1 overflow-x-auto">{h.map((_,i)=><button key={i} onClick={()=>restore(i)} className="rounded border bg-white px-2 py-1 text-[10px]">v{i+1}</button>)}</div></div>}
